import React from 'react';
import PropTypes from 'prop-types';

const AllFood = props => {
    return (
        <div>
            <h1>all food</h1>
        </div>
    );
};

AllFood.propTypes = {
    
};

export default AllFood;