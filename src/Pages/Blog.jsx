import React from 'react';
import PropTypes from 'prop-types';

const Blog = props => {
    return (
        <div>
            <h1>blog</h1>
        </div>
    );
};

Blog.propTypes = {
    
};

export default Blog;